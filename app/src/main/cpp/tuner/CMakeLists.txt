AUX_SOURCE_DIRECTORY(. LIB_DCAPP_SRC)

ADD_LIBRARY(dcapp STATIC ${LIB_DCAPP_SRC})

INCLUDE_DIRECTORIES(9x falcon rds)

ADD_SUBDIRECTORY(9x)
ADD_SUBDIRECTORY(rds)
#ADD_SUBDIRECTORY(falcon)

SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)

TARGET_LINK_LIBRARIES(dcapp platform)
