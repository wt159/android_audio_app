#SET(LIBSI479XXCORE_SRC si479x_audio.c)
AUX_SOURCE_DIRECTORY(. LIBSI479XXCORE_SRC)

INCLUDE_DIRECTORIES(..)

ADD_LIBRARY (si479x ${LIBSI479XXCORE_SRC})

SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)
